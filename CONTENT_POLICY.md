# 内容来源与公开边界

代码许可证为 MIT。每条 Prompt 和每份媒体的来源/许可独立记录；代码开源不自动改变外部案例、照片或生成结果的使用条件。

原创记录保留真实 Prompt、实际结果、已知模型信息、来源工具和日期。收集/改写外部提示词应标明作者、原始链接、改写情况与许可，未知时填写 `unspecified`，不得自动展示“可商用”。生成结果有后期改动时在 `postProcessing` 与备注说明；缩略图/非生成式压缩不当成新的模型结果。

上传前确认：没有密钥、账号信息、个人敏感资料、未授权公开的参考照片或文件元数据。尤其不要把完整 ChatGPT 私人对话 URL / 内部资料作为默认公开来源；用户主动提供的公开链接才可写入 source。

公开记录、Git 历史、公开媒体 URL 可能被复制。`archived` 只是列表隐藏；不是私密存储或保证彻底删除。收到异议时先暂停展示，再核对来源并按需要删除 R2 对象/缓存及处理 Git 历史，无法承诺撤回他人已保存的副本。

本包 `prototype/` 中的视觉与视频均是界面测试素材，明确标记为非模型实测；不得将这些样例导入正式内容库并给它们编造模型来源。用户提供的参考截图未收录进可公开仓库。
