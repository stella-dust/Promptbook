import { test } from 'node:test';import assert from 'node:assert/strict';import { onRequest } from '../infra/blog-proxy.js';
test('blog integration rejects other hostnames and unrelated paths',async()=>{for(const path of ['https://junyiyan-blog.pages.dev/projects/promptbook/','https://junyiyan.com/projects/promptbook-other','https://junyiyan.com/']){const result=await onRequest({request:new Request(path),env:{PROMPTBOOK:{fetch:()=>{throw new Error('unexpected dispatch')}}}});assert.equal(result.status,404)}});
test('narrow blog integration preserves URL and request',async()=>{const req=new Request('https://junyiyan.com/projects/promptbook/?q=hello');const result=await onRequest({request:req,env:{PROMPTBOOK:{fetch:(r:Request)=>{assert.equal(r,req);return new Response('static')}}}});assert.equal(await result.text(),'static')});
