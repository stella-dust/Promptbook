# DESIGN · 安静的视觉手记

## 1. 设计意图

像一本被认真整理的作品手记：结果先被看见，Prompt 随手可读、可复制。界面只承担整理工作，不抢作品的视觉注意力。

以用户给出的 prompts.chat 截图为信息关系参考：图片 + 标题 + Prompt + 分类。删除其长顶部导航、左侧密集筛选、广告、社交信息和功能入口。不要复刻其覆盖图片底部的白色渐隐：生成结果需要完整、真实地被观察。

本包原型确定的是布局、密度、阅读节奏和交互位置。原型里的测试插画不作为站点的最终内容风格；真正的视觉主角是后续上传的结果。

## 2. 视觉语言

- 浅暖白背景，深灰正文，细分割线，低饱和辅助色。
- 小圆角、低装饰度；不用厚阴影、渐变背景、玻璃态、霓虹和悬浮发光。
- 中文使用系统无衬线；英文站名可以使用系统衬线，形成手记感。无需外部字体请求。
- 正文和控件左对齐。窄元信息可以等宽；Prompt 不使用大块“代码终端”皮肤。
- 页面标题短，最多一句说明。首页没有营销大 Hero、指标卡或仪表盘。

## 3. Tokens

```css
:root {
  --canvas: #faf9f6;
  --surface: #ffffff;
  --ink: #252522;
  --muted: #797974;
  --line: #e6e4de;
  --subtle: #f0efea;
  --accent: #53624f;
  --danger: #a34238;
  --radius-control: 6px;
  --radius-media: 8px;
  --page-max: 1440px;
  --font-ui: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
  --font-brand: Georgia, "Times New Roman", serif;
  --font-mono: "SFMono-Regular", Consolas, monospace;
}
```

色值可在视觉验收时微调。对比度优先于“浅淡高级感”；常规小字号正文与背景达到 WCAG AA 对比度，禁用态之外不通过降低 opacity 隐藏信息。

## 4. 页面骨架

桌面 1440px：两侧各 56–64px；最大内容宽 1440px。顶部栏高 72px，下边 1px 线。站名左侧，右侧 GitHub 贡献入口和“新建记录”。公开访客不展示编辑控件；原型为了评审默认展示维护者视角。正式公开页的管理入口放页脚，维护者从 `/admin` 进入后可获得自己的工具栏，不让公开首页为识别账号发起鉴权请求。

标题区 64px 左右的上间距，标题 32–36px / 1.3，说明 13–14px，控制整体高度，1440×1000 首屏必须看到第一排真实作品和标题。

筛选区两行：第一行“全部 / 图片 / 视频”与搜索、模型、排序；第二行轻量分类标签。选中分类采用深色底或细下划线，其他只显示文本或极浅底，不建立几十个 pill。没有左侧永久筛选栏。

## 5. 首页卡片

桌面 >=1280px 四列；900–1279px 三列；600–899px 两列；<600px 单列。列间距 24px，卡片间纵向 28–36px。

使用自然比例的瀑布流。**不裁掉九宫格的下半部分、不把横幅放大裁成人像。** 特别长的图片可限制预览最大高度并明确“查看完整结果”，详情始终 contain。卡片宽度不足时不要强行显示全部参数。

卡片层级：

1. 图片/视频封面，8px 圆角；无厚边框、无渐隐。视频右上角轻标识时长，组图显示数量。
2. 标题 15px、medium，最多两行；标题区与图片间隔 12px。
3. Prompt 前两行预览，12–13px / 1.65；保留完整原文在详情；截断不改变复制结果。
4. 模型显示名 + 分类/日期，小号灰字；复制按钮固定于卡片下方右侧。hover 与键盘 focus 都可触达，手机始终显示。

整张图/标题打开详情，复制按钮独立，不嵌套按钮、不把复制动作变成跳转。用户滚动浏览再返回时恢复原筛选和滚动位置。

瀑布流实现必须保留合理 DOM/键盘顺序。禁止 JS 将记录按四个独立竖列重排导致读屏与视觉顺序混乱。可采用 measured CSS Grid；移动单列完全按排序顺序。

## 6. 详情页

路径 `/p/{id}/`。桌面左右分栏约 58:42，gap 48px；整体与首页同宽。上方只保留“返回手记”和维护者编辑入口。

左侧：浅中性画布中的完整媒体；原始宽高比，长图可上下浏览；组图/多个输出使用缩略图切换。视频原生可控播放器，有封面、进度、音量、全屏；不自动播放。点击图片进入原尺寸查看模式，Escape 退出并恢复焦点。

右侧：分类小字 → 标题 → 模型/平台 → Prompt 全文 + 复制 → 原始生成信息（默认折叠）→ 备注/来源。

Prompt 容器浅底、1px 线、6px 圆角；宽度可阅读，`white-space: pre-wrap`，英文长词允许换行。超过约 24 行时折叠，但复制永远取存储的全文；显示“展开全文”。多轮追加提示独立列出，不偷偷拼接或覆盖初始 Prompt。

“生成信息”只展示实际记录的字段。未知值显示“未记录”；不把产品名当成已确认的 API 模型 ID。原图下载入口显示文件类型、大小；图像预览转换不冒充原图。

移动端：媒体在前，Prompt 在后；标题和复制入口在一屏左右可到达。不要把全部参数表放到 Prompt 前面。

## 7. 新建 / 编辑页

路径 `/admin/new/`、`/admin/edit/?id=...`，由 Access 保护。桌面编辑主栏约 60%，结果预览约 40%；手机单栏。

顶部“新建记录 / 编辑记录”，右侧草稿保存状态。第一屏只有：标题、Prompt、结果上传、分类、平台/模型。先进参数默认折叠。

输入优先：粘贴 Prompt；拖入 / 粘贴实际结果；系统读出尺寸、格式、大小、时长；记住上一次分类和模型，但允许清空。用户未确认的平台/模型不得从图片里臆测。

上传区支持点击、拖放和剪贴板图片；能看见缩略图、上传进度、移除、设为封面。视频上传显示封面与时长。图片与视频混合时提示拆成两条记录，V1 同条记录只接收同一种输出类型。

高级信息：模型 ID、模型证据、生成日期、其他参数 JSON、负向 Prompt、追加提示、参考输入、来源/署名、许可、备注。参考输入默认不公开上传；主动选择并二次确认才进入公开记录。

底部 sticky 操作条：保存草稿 / 预览 / 发布。编辑已有记录显示“发布更新”。此处不出现 AI 优化 Prompt、生成按钮或 Token 消费。

发布状态：检查 → 准备媒体 → 上传 2/5 → 验证媒体 → 写入 GitHub → 等待网站更新 → 已上线。失败停在对应步骤，保留所有表单和已成功上传的文件信息。

## 8. 状态与微交互

- 空库：一句说明、“新建第一条记录”、次级 GitHub 贡献说明；不自动塞入假的模型作品。
- 无搜索结果：显示当前过滤条件和“清除筛选”，保留输入。
- 加载：固定尺寸中性占位，避免布局跳动；禁止循环闪烁大骨架占据整屏。
- 复制：按钮短暂变为“已复制”，约 1.5s 恢复，读屏 live region 也通知。
- Toast：底部单行，不堆叠；错误同时就近显示到字段/文件。
- 动画 100–160ms，仅 opacity/细微位移；尊重 prefers-reduced-motion。无图片悬停放大、无视差。
- 所有图标按钮有中文 aria-label；触摸目标 >=44×44px，复制区视觉尺寸可以更小，点击区不能更小。

## 9. 视觉验收

第一眼看到的是作品，不是控件；首屏没有大片解释和空白 Hero。分类有层次但不喧宾夺主。真实九宫格、竖图、横图、超长 Prompt、中文长标题、视频封面均不破坏版式。

需提供 1440×1000、1280×800、768×1024、390×844、360×800 截图；检查横向溢出、标题换行、按钮触达和底部操作条遮挡。交互细节以 `docs/ACCEPTANCE.md` 为准。

## 10. 正式应用的排版深化 · 2026-09-20
保留暖白、墨绿、作品先行和原型功能位置。英文站名使用更有分量的衬线字与句点，中文标题保留克制尺度；用通栏细线、书页式边距、卡片留白和紧凑的底部题注建立编辑感。空库采用一页未写的手记，不使用虚构作品占位。移动端优先减小外围留白，保留 44px 操作触点。公开导航只有手记和贡献，管理在页脚。
